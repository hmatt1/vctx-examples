# Promotion tracker: sim expected-fails that should become passing tests

These `sim_*_should_work.vctx` files intentionally fail **today** and serve as
living documentation for compiler work that remains.

Promotion rule:
- Keep file in `on_purpose_failures_sim/` while it fails.
- Move to regular passing examples/tests once behavior is implemented and stable.
- Remove from `_SIM_FIRST` (and `_SIM_ALSO` if present) in `vctx-lang/tests/test_on_purpose_primary_codes.py` at the same time you delete the xfail file.

**Candidates to move to passing sims later:** every row below. When a row’s “Promotion trigger” is met, that file is ready to **graduate** out of this directory (not to remain as an xfail).

## By failure stage (how sim fails today)

| Stage | Typical first `[E_…]` | What it means for implementers |
|--------|------------------------|--------------------------------|
| **Precheck** | `E_SIM_PRECHECK_FAILED` | Static pipeline failed before sim compile; often with `E_COMPTIME_REQUIRED` for parametric carriers, or `E_TYPE_UNKNOWN` when comptime evaluation hits an unsupported node. |
| **Sim compile** | `E_SIM_COMPILE_FAILED` | Late compile (e.g. width folding); fix folding/typecheck before runtime. |
| **Runtime assert** | `E_SIM_ASSERTION_FAILED` | Reached sim but value wrong (comptime/concat/intrinsic gap). |
| **Poke / harness** | `E_SIM_POKE_TARGET_INVALID` / `E_SIM_PRECHECK_FAILED`+`E_UNKNOWN_IDENTIFIER` | Testbench rules; not “should work” promotion cases unless renamed. |

| File | Area | Intended behavior (future pass) | Why this exists now | Promotion trigger |
|---|---|---|---|---|
| `sim_concat_basic_should_work.vctx` | Intrinsics / lowering | `concat(high, low)` packs into `0xDEAD` | Documents current concat runtime mismatch | Assert passes and stays stable in regular sim suite |
| `sim_concat_variadic_should_work.vctx` | Intrinsics / lowering | `concat(a,b,c,d)` on four `u4` packs into `0xABCD` | Isolates variadic concat (today `out` is 0) | Four-nibble concat matches expected `u16` in sim |
| `sim_comptime_nested_call_should_work.vctx` | Comptime functions | comptime `outer` calls comptime `inner` and composes to `u32` | Comptime folder does not support `function_call` node in inner position yet (`E_TYPE_UNKNOWN` on line) | Nested comptime calls fold without precheck error |
| `sim_comptime_recursion_should_work.vctx` | Comptime evaluator | comptime `while`-loop factorial should fold to 120 (file name keeps the “recursion” promotion target; direct `if`/`fact(n-1)` is not parseable here yet) | Today `w` is 0; documents comptime while/multiplication path for driving wires | `fact_iter(5)` folds to 120 (or replace body with true recursion once supported) |
| `sim_slice_concat_byteswap_should_work.vctx` | Slicing + concat | `concat(x[7..0], x[15..8])` on `u16` should byte-swap to `0x3412` | Today `y` is 0; same class of gap as `sim_concat_basic_should_work` but adds slices | Byte-swap assert passes in sim |
| `sim_intrinsics_is_comptime_should_work.vctx` | Comptime intrinsics | `is_comptime` is true for folded constants | Locks current semantic gap in constant-ness reporting | `is_comptime` behavior matches language contract in sim |
| `sim_comptime_clog2_should_fold.vctx` | Comptime functions | comptime `std.clog2(16) == 4` | Guards missing/incorrect comptime fn folding | Known value folds correctly under sim |
| `sim_comptime_eval_cov_should_work.vctx` | Comptime evaluator | comptime loop/arithmetic evaluation returns expected value | Tracks evaluator coverage gap | Expression/statement evaluator returns expected runtime literal |
| `sim_comptime_loop_control_should_work.vctx` | Comptime evaluator | comptime `break/continue` semantics evaluate correctly | Isolates loop-control semantics regression | Control-flow semantics match expected folded value |
| `sim_comptime_value_containers_should_work.vctx` | Comptime containers | comptime Array/Map indexing returns expected values | Tracks container access folding gap | Array/map literals + indexing fold deterministically |
| `sim_comptime_len_array_should_work.vctx` | Comptime containers | `len([...])` on a comptime array literal should fold to correct length | Today `w` is 0 at runtime; documents `len()`/literal-array folding gap for driving wires | `len` on comptime array literal folds to 3 in this harness |
| `sim_zero_extend_generic_should_work.vctx` | Generic functions | generic function call resolves and widens correctly | Tracks generic call/value path in sim | Generic function call passes with expected value |
| `sim_parametric_carrier_component_should_work.vctx` | Parametric carriers | `u<WIDTH>` component ports specialize at sim/check/lowering | Documents that carrier-width generics still fail precheck | `u<WIDTH>` ports typecheck+simulate with no precheck errors |
| `sim_parametric_carrier_pipeline_should_work.vctx` | Parametric carriers | nested `u<W>` generic carriers flow through hierarchy | Ensures hierarchical specialization is covered | Nested generic carriers elaborate and pass assertions |
| `sim_parametric_carrier_expr_should_work.vctx` | Parametric carriers | derived width `u<(W + 1)>` specializes from Int generics | Tracks expression-based carrier-width folding gap | Carrier-width expressions fold during specialization |
| `sim_parametric_array_carrier_should_work.vctx` | Parametric carriers + arrays | arrays with `u<W>` element types specialize in sim path | Captures combined generic-carrier+array typing/lowering gap | `u<W>[N]` forms typecheck/simulate and assertion passes |
| `sim_parametric_carrier_function_should_work.vctx` | Parametric carriers + functions | generic function signatures with `u<W>` should specialize | Captures function-level generic carrier gap (not just components) | Function `u<W>` param/return paths pass under sim |
| `sim_parametric_two_width_params_should_work.vctx` | Parametric carriers | two independent `A`, `B` in `u<A>`, `u<B>` and `u<(A+B)>` in one unit | Extends single-parameter `W` cases to multi-argument width generics | `TwoWidths<A,B>` typechecks, elaborates, and `concat`+assert pass |
| `sim_parametric_carrier_nested_expr_should_work.vctx` | Parametric carriers | nested Int expressions like `u<(W+W)>` should fold | Ensures nested expression folding is tracked separately from `W+1` | Nested width expressions specialize and simulate correctly |
| `sim_parametric_signed_carrier_should_work.vctx` | Parametric carriers (signed) | signed carriers `s<W>` should specialize like `u<W>` | Tracks signed parametric carrier support separately from unsigned | `s<W>` generic ports typecheck/simulate and assertion passes |
| `sim_parametric_mixed_arith_should_work.vctx` | Parametric arithmetic | mixed arithmetic result widths like `u<(W+1)>` should fold and cast correctly | Captures arithmetic + width-specialization path in one minimal case | Arithmetic over generic widths lowers and passes stable assertion |
| `sim_parametric_slice_should_work.vctx` | Parametric carriers + slicing | slices over `u<W>` should specialize and produce typed slices | Tracks slice typing/lowering path for parametric carriers | `u<W>` slicing typechecks and passes stable nibble assertion |
| `sim_parametric_signed_cast_should_work.vctx` | Parametric carriers + casts | casts between `s<W>` and `u<W>` should specialize cleanly | Isolates signed/unsigned generic-cast behavior | Generic signed/unsigned cast path passes deterministic assertion |
| `sim_parametric_concat_should_work.vctx` | Parametric carriers + concat | `concat(u<W>, u<W>)` should specialize to `u<(W+W)>` | Tracks concat typing/lowering over generic carriers | Concat over specialized generic widths passes stable assertion |
| `sim_parametric_width_intrinsic_should_work.vctx` | Parametric carriers + intrinsics | `width(u<W>)` should fold after specialization | Captures compile-stage gap where width does not fold and emits `E_SIM_COMPILE_FAILED` | `width(...)` folds in sim path with no compile-stage bail-out |
| `sim_array_literal_init_should_work.vctx` | Arrays + wire init | `wire a: T[N] = […, …]` in a `sim` block, then `a[i]` in assertions | Today precheck: `E_SIM_PRECHECK_FAILED` and `E_TYPE_UNKNOWN` (literal not unified with `u8[4]`; diagnostics may show nested dimensions) | Literal initializer typechecks, sim runs, index assert passes; remove from `_SIM_FIRST` / `_SIM_ALSO` |
| `sim_reg_array_index_should_work.vctx` | Reg arrays + indexing | `reg` array with `a[k] <= …` and read `a[j]` to drive output | Today precheck: `E_SIM_PRECHECK_FAILED` and `E_WIDTH_MISMATCH` (indexed `reg` element treated as `u1` LHS) | `reg` array element width/index semantics match `u8`; sim assert passes; then graduate |
| `sim_wire_array_element_assign_should_work.vctx` | Wire arrays + element assign | `wire` array, per-element `:=` into `t[k]`, read another element | Same width bug as `sim_reg_array_index_should_work` but for **combinational** `:=` to `wire` array elements (precheck: `E_WIDTH_MISMATCH`) | Per-element `wire` array updates get correct width; sim assert passes; **promotion candidate** (often fixed together with reg arrays) |
| `sim_dynamic_bracket_slice_should_work.vctx` | Slicing (dynamic) | `w[hi..lo]` with `hi`/`lo` from ports/wires, fixed intended slice value in assert | Precheck: `E_SIM_PRECHECK_FAILED` and `E_TYPE_UNKNOWN` (mirrors `on_purpose_failures_check/type_dynamic_bracket_slice_unknown_width.vctx` but targets the **sim** queue) | Dynamic bracket slices have known width in analysis and sim produces correct `u8`; file graduates to e.g. `operators/` sims |
| `sim_ternary_runtime_arm_mismatch_should_work.vctx` | Ternary (runtime) | Incompatible `u8` / `u16` arms on a **non-constant** `cond` should be rejected or use explicit casts | Precheck: `E_SIM_PRECHECK_FAILED` and `E_TERNARY_ARM_MISMATCH` (sim promotion twin: `on_purpose_failures_check/type_ternary_arm_type_mismatch.vctx` uses the same check shape in `check`) | Replace with a **passing** sim (matching arms or casts) under normal examples, or delete if check-only is enough; remove from pytest maps when stable |
| `sim_ternary_runtime_signedness_mismatch_should_work.vctx` | Ternary (runtime, signedness) | Same-width ternary arms with mismatched signedness (`u8` vs `s8`) should be rejected or normalized explicitly | Precheck: `E_SIM_PRECHECK_FAILED` and `E_TERNARY_ARM_MISMATCH` (signedness-focused companion to ternary width mismatch case) | Replace with a **passing** sim using explicit cast/normalization policy, then remove xfail and update maps |
| `sim_signedness_assignment_should_work.vctx` | Signedness / casts | Assignment from `s8` producer to `u8` sink should be explicit and deterministic | Precheck: `E_SIM_PRECHECK_FAILED` and `E_TYPE_SIGN_MISMATCH` (sim promotion twin: `on_purpose_failures_check/type_signedness_mismatch_assignment.vctx`) | Move to a **passing** sim with explicit cast (or updated language rule), then delete xfail and drop from `_SIM_FIRST` / `_SIM_ALSO` |
| `sim_width_mismatch_assignment_should_work.vctx` | Width / casts | Assignment from `u16` producer to `u8` sink should be explicit and deterministic | Precheck: `E_SIM_PRECHECK_FAILED` and `E_WIDTH_MISMATCH` (sim promotion twin: `on_purpose_failures_check/type_width_mismatch_assignment.vctx`) | Move to a **passing** sim with explicit cast/truncate policy, then delete xfail and drop from `_SIM_FIRST` / `_SIM_ALSO` |
| `sim_output_signedness_mismatch_should_work.vctx` | Output signedness / casts | Component drives unsigned `out` from signed internal source; boundary intent should be explicit | Precheck: `E_SIM_PRECHECK_FAILED` and `E_TYPE_SIGN_MISMATCH` (output-boundary complement to assignment/port signedness xfails) | Move to a **passing** sim with explicit cast/adapter at output boundary, then delete xfail and drop from `_SIM_FIRST` / `_SIM_ALSO` |
| `sim_output_width_mismatch_should_work.vctx` | Output width / casts | Component drives narrow `out u8` from wider internal `u16`; boundary truncation intent should be explicit | Precheck: `E_SIM_PRECHECK_FAILED` and `E_WIDTH_MISMATCH` (output-boundary complement to assignment/port width mismatch xfails) | Move to a **passing** sim with explicit cast/truncate at output boundary, then delete xfail and drop from `_SIM_FIRST` / `_SIM_ALSO` |
| `sim_port_input_signedness_reverse_mismatch_should_work.vctx` | Port signedness / casts (reverse direction) | Instance wiring from `u8` source into `s8` input should be explicit at boundary | Precheck: `E_SIM_PRECHECK_FAILED` and `E_TYPE_SIGN_MISMATCH` (directional companion to signed→unsigned port mismatch coverage) | Move to a **passing** sim with explicit cast adapter and remove from xfail maps; if check handles it fully, keep only check-side regression |
| `sim_port_input_width_mismatch_should_work.vctx` | Port width / casts (input boundary) | Instance wiring from `u8` source to `u16` input should be explicit at boundary | Precheck: `E_SIM_PRECHECK_FAILED` and `E_PORT_WIDTH_MISMATCH` (precheck-stage complement to compile-stage `sim_port_width_instance_mismatch_sim_compile_should_work`) | Move to a **passing** sim with explicit widen/cast adapter and remove from xfail maps; if check handles it fully, keep only check-side regression |
| `sim_port_input_width_narrowing_mismatch_should_work.vctx` | Port width / casts (input boundary, reverse direction) | Instance wiring from `u16` source to `u8` input should be explicit at boundary | Precheck: `E_SIM_PRECHECK_FAILED` and `E_PORT_WIDTH_MISMATCH` (directional companion to `sim_port_input_width_mismatch_should_work`) | Move to a **passing** sim with explicit narrow/cast adapter and remove from xfail maps; if check handles it fully, keep only check-side regression |
| `sim_port_input_width_signedness_combo_should_work.vctx` | Port width + signedness combo (input boundary) | Instance wiring from `s16` source to `u8` input should be explicit at boundary | Precheck: `E_SIM_PRECHECK_FAILED` and `E_PORT_WIDTH_MISMATCH` (combo mismatch currently reports width first) | Move to a **passing** sim with explicit cast/adapter for both width and signedness intent, then remove xfail mapping |
| `sim_port_output_width_signedness_combo_should_work.vctx` | Port width + signedness combo (output boundary) | Instance wiring from `s16` producer into `u8` sink should be explicit at boundary | Compile stage: `E_SIM_COMPILE_FAILED` (MLIR/CIRCT output type mismatch; combo companion to input-boundary combo) | Move to a **passing** sim with explicit cast/adapter at the sink boundary, then remove xfail mapping |
| `sim_port_output_width_mismatch_should_work.vctx` | Port width / casts (output boundary) | Instance wiring from `u16` producer into `u8` sink should be explicit at boundary | Compile stage: `E_SIM_COMPILE_FAILED` (MLIR/CIRCT output type mismatch; output-boundary companion to input-boundary/precheck coverage) | Move to a **passing** sim with explicit narrow/cast adapter and remove from xfail maps; if check handles it fully, keep only check-side regression |
| `sim_port_signedness_instance_mismatch_should_work.vctx` | Port signedness / casts | Component boundary wiring from `s8` producer to `u8` sink should be explicit and deterministic | Precheck: `E_SIM_PRECHECK_FAILED` and `E_TYPE_SIGN_MISMATCH` (port-level signedness mismatch in sim; complements assignment-level signedness twins) | Move to a **passing** sim with explicit cast/adapter at boundary, then delete xfail and drop from `_SIM_FIRST` / `_SIM_ALSO` |
| `sim_reg_comb_assignment_should_work.vctx` | Assignment discipline (`reg` + `:=`) | Register updates should use sequential `<=`, not combinational `:=` | Precheck: `E_SIM_PRECHECK_FAILED` and `E_ASSIGN_OP_INVALID` (sim promotion twin: `on_purpose_failures_check/type_reg_assigned_with_comb_op_in_root.vctx`) | Keep a passing replacement in normal examples using `<=`; delete xfail + map entries once promoted |
| `sim_wire_seq_assignment_should_work.vctx` | Assignment discipline (`:=` vs `<=`) | `wire`-driven outputs should use combinational `:=` and reject sequential `<=` misuse | Precheck: `E_SIM_PRECHECK_FAILED` and `E_ASSIGN_OP_INVALID` (sim promotion twin: `on_purpose_failures_check/type_wire_assigned_with_seq_op.vctx`) | Keep a passing replacement in normal examples with `:=`; delete xfail + map entries once promotion is complete |
| `sim_port_width_instance_mismatch_sim_compile_should_work.vctx` | Port wiring + sim compile | `u16` component output into a `u8` `sim` `wire` — illegal connection | Today `E_SIM_COMPILE_FAILED` from MLIR/CIRCT (hw.output type mismatch) after sim compile — related to `on_purpose_failures_check/type_bad_port_connection_width.vctx` (instance `u16` → `u8` port) | **Promotion:** fix design (truncate/widen + matching ports) to get a **passing** sim, and/or make `vctx check` own this so sim never sees it — then **delete** this xfail |

## Index: `sim_*_should_work` by theme (for maintenance)

| Theme | Files |
|--------|--------|
| **Concat / slice** | `sim_concat_basic_should_work`, `sim_concat_variadic_should_work`, `sim_slice_concat_byteswap_should_work`, `sim_parametric_concat_should_work` |
| **Comptime** | `sim_comptime_clog2_should_fold`, `sim_comptime_eval_cov_should_work`, `sim_comptime_loop_control_should_work`, `sim_comptime_value_containers_should_work`, `sim_comptime_len_array_should_work`, `sim_comptime_nested_call_should_work`, `sim_comptime_recursion_should_work` |
| **Intrinsics** | `sim_intrinsics_is_comptime_should_work`, `sim_parametric_width_intrinsic_should_work` |
| **Parametric `u<W>` / `s<W>`** | all `sim_parametric_*_should_work` except concat-only row above, plus `sim_zero_extend_generic_should_work` |
| **Arrays / `reg` / `wire` index + assign** | `sim_array_literal_init_should_work`, `sim_reg_array_index_should_work`, `sim_wire_array_element_assign_should_work` |
| **Dynamic `w[hi..lo]` slice (runtime bounds)** | `sim_dynamic_bracket_slice_should_work` (see also check-only twin: `type_dynamic_bracket_slice_unknown_width.vctx`) |
| **Type discipline (signedness / width / ternary / assignment / port width)** | `sim_signedness_assignment_should_work` (check twin: `type_signedness_mismatch_assignment.vctx`), `sim_output_signedness_mismatch_should_work` (output-boundary signedness), `sim_port_signedness_instance_mismatch_should_work` (port-level signedness mismatch), `sim_port_input_signedness_reverse_mismatch_should_work` (reverse-direction input signedness mismatch), `sim_width_mismatch_assignment_should_work` (check twin: `type_width_mismatch_assignment.vctx`), `sim_output_width_mismatch_should_work` (output-boundary width mismatch), `sim_port_input_width_mismatch_should_work` (input-boundary width mismatch), `sim_port_input_width_narrowing_mismatch_should_work` (reverse-direction input width mismatch), `sim_port_input_width_signedness_combo_should_work` (input-boundary combo mismatch), `sim_port_output_width_signedness_combo_should_work` (output-boundary combo mismatch, compile-stage), `sim_port_output_width_mismatch_should_work` (output-boundary width mismatch, compile-stage), `sim_ternary_runtime_arm_mismatch_should_work` (check twin: `type_ternary_arm_type_mismatch.vctx`), `sim_ternary_runtime_signedness_mismatch_should_work` (signedness-focused ternary mismatch), `sim_reg_comb_assignment_should_work` (check twin: `type_reg_assigned_with_comb_op_in_root.vctx`), `sim_wire_seq_assignment_should_work` (check twin: `type_wire_assigned_with_seq_op.vctx`), `sim_port_width_instance_mismatch_sim_compile_should_work` (compare `type_bad_port_connection_width.vctx`) |

**Passing-sim target:** any row whose promotion trigger is met should leave `on_purpose_failures_sim/` and live under normal `vctx-examples` packages (or project tests), not stay as an xfail. Every table row and checklist item is a **promotion candidate**: once the trigger is met, the file is deleted from this suite and removed from the pytest primary-code maps; it should **not** remain as an xfail.

