// Expected failure: bracket slice ``w[hi..lo]`` with both bounds from wires — slice width
// is not known at analysis time; assignment is ``E_TYPE_UNKNOWN`` (see dynamic_slicing_proposal.md).

component DynBracketSlice(in hi: u4, in lo: u4, in w: u16, out z: u8) {
    z := w[hi..lo]
}
