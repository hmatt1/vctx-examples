// spec: §5.1
// expect: pass check; pass mlir
// ``inout`` on a port: readable on the RHS like ``in``; block argument has type ``!hw.inout<T>``, read via ``sv.read_inout``.
component PassThru(inout pin: u8, out q: u8) {
    q := pin
}
